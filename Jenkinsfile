pipeline {
    agent {
        docker { image 'idempiereofficial/idempiere:source-release-11.0' }
    }

    stages {
        stage('Compile') {
            steps {
                sh 'ls'
            }
        }
    }
}
